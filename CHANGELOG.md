# تغییرات این نسخه (v2.0.0)

## 🔄 سازگاری با ساختار جدید مکتب‌خونه (مهم‌ترین تغییر)
- تشخیص خودکار API قدیم/جدید مکتب‌خونه، به ازای هر دوره (کش‌شده، بدون افت سرعت محسوس)
- پشتیبانی از هر دو ساختار آدرس: `/course/<slug>/...` (قدیم) و `/lms/course/<slug>/unit/<id>/` (جدید)
- افزودن هدر `x-csrftoken` که سایت جدید برای همه‌ی درخواست‌های API الزامی کرده
- استخراج ویدیو/زیرنویس مستقیماً از JSON (به‌جای regex روی HTML) در دوره‌های جدید

## ⚙️ فلگ‌های جدید خط فرمان
- `--quality <کیفیت>` — انتخاب کیفیت دلخواه با fallback به نزدیک‌ترین کیفیت پایین‌تر
- `--parallel <1-3>` — دانلود چند ویدیوی هم‌زمان
- `--chapters <بازه/لیست>` — محدود کردن دانلود به فصل‌های خاص
- `--no-subtitle` / `--no-attachments` — رد کردن دانلود زیرنویس/پیوست
- `--no-log` — غیرفعال‌کردن فایل لاگ (پیش‌فرض فعال است)
- `--estimate-size` — تخمین حجم کل دوره قبل از دانلود
- `--dry-run` — فقط لیست‌کردن آنچه دانلود می‌شود، بدون دانلود واقعی
- `--retry-failed` — تلاش مجدد فقط برای موارد ناموفق یک اجرای قبلی
- `--verify-integrity` — بررسی صحت فایل‌های دانلودشده و پیشنهاد دانلود مجدد موارد مشکوک
- `--output <path>` — مسیر خروجی دلخواه (مخصوصاً برای موبایل)
- `--no-wake-lock` / `--no-notify` — کنترل قابلیت‌های Termux

## 📊 گزارش‌گیری
- تولید خودکار `report.csv` و `report.html` در پایان هر اجرای دانلود
- ستون `DownloadURL` با لینک مستقیم هر ویدیو + هشدار انقضای توکن (۱۵ تا ۳۰ دقیقه) + زمان دقیق تولید گزارش
- فایل `dry-run-list.csv` مخصوص حالت `--dry-run`
- خلاصه‌ی محتوای غیر ویدیویی (کوییز/پروژه/...) که ابزار دانلودشون نمی‌کنه

## 📱 پشتیبانی از موبایل (اندروید/Termux)
- راهنمای کامل نصب و اجرا روی Termux در README
- Wake-lock خودکار حین دانلود (جلوگیری از خواب رفتن گوشی)
- نوتیفیکیشن پایان کار با خلاصه‌ی نتیجه
- مسیر خروجی قابل تنظیم برای ذخیره مستقیم در پوشه‌ی Download گوشی

## 🛡️ پایداری و کارایی
- Retry هوشمند روی خطای ۴۲۹ (Too Many Requests) با رعایت هدر `Retry-After`
- ادامه‌ی دانلود از همان‌جا (Resume) حفظ شد و با حالت‌های جدید سازگار شد

## 👤 سایر
- به‌روزرسانی اطلاعات نگهدارنده به HaamiData (با حفظ اعتبار نویسنده‌ی اصلی طبق GPL v3.0)
